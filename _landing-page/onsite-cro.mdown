---
layout: landing-page
title: 'Conversion Rate Optimization'
slug: onsite-cro
path: ../

meta-title: 'CRO by Alt House'
meta-description: 'CRO by Alt House drive sales.'

item-order: 2
---
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Barlow:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg:         #0f0e0d;
      --fg:         #f0ebe2;
      --card:       #191714;
      --secondary:  #242118;
      --muted-fg:   #B9AF9C;
      --accent:     #c8a84b;
      --border:     rgba(240,235,226,0.10);
      --sans:       'Barlow', sans-serif;
      --display:    'Barlow', sans-serif;
      --mono:       'DM Mono', monospace;

      --pad-section-sm: 6rem;
      --pad-section: 9rem;
      --pad-section-lg: 11rem;
    }
    /* override althouse */
    @media (min-width: 420px) {
        .hero h1, .hero h2 {
            max-width: 100%;
        }
    }

    html { font-size: 16px; scroll-behavior: smooth; }

    body {
      background: var(--bg);
      color: var(--fg);
      font-family: var(--sans);
      -webkit-font-smoothing: antialiased;
      min-height: 100vh;
    }
    p {
        margin: 0;
    }
    h1, h2 {
        font-weight: 700;
    }

    /* ─── Scrollbar ───
    ::-webkit-scrollbar { width: 6px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }
    */

    /* ─── Helpers ─── */
    .container { max-width: 87rem; margin: 0 auto; padding: 0 1.5rem; }
    @media (min-width: 1024px) { .container { padding: 0 3rem; } }

    .label {
      font-family: var(--mono);
      font-size: 0.6875rem;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: var(--accent);
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .label::before, .label.center::after {
      content: '';
      display: inline-block;
      width: 1.5rem;
      height: 1px;
      background: var(--accent);
      flex-shrink: 0;
    }

    a { color: inherit; text-decoration: none; }

    /* ─── NAV ─── */
    header {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 50;
      transition: background 0.4s, border-color 0.4s, backdrop-filter 0.4s;
    }
    header.scrolled {
      background: rgba(15,14,13,0.9);
      border-bottom: 1px solid var(--border);
      backdrop-filter: blur(12px);
    }
    nav-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      height: 4rem;
    }
    .wordmark {
      font-family: var(--display);
      font-weight: 300;
      font-size: 1rem;
      letter-spacing: -0.01em;
    }
    .nav-links {
      display: flex;
      align-items: center;
      gap: 2rem;
      list-style: none;
    }
    .nav-links a {
      font-size: 1rem;
      color: var(--muted-fg);
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--fg); }
    .btn-primary {
      background: var(--accent);
      color: var(--bg);
      font-family: var(--sans);
      font-size: 1rem;
      font-weight: 500;
      padding: 0.5rem 1.25rem;
      border: none;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: opacity 0.2s;
      text-decoration: none;
    }
    .btn-primary:hover { opacity: 0.85; }
    .btn-primary svg, .btn-outline svg { transition: transform 0.2s; }
    .btn-primary:hover svg, .btn-outline:hover svg { transform: translateX(3px); }

    .btn-outline {
      border: 1px solid var(--border);
      color: var(--fg);
      font-family: var(--sans);
      font-size: 1rem;
      padding: 0.75rem 1.25rem;
      display: inline-flex;
      align-items: center;
      gap: 0.75rem;
      transition: border-color 0.2s, color 0.2s;
      background: transparent;
      cursor: pointer;
      text-decoration: none;
    }
    .btn-outline:hover { border-color: var(--accent); color: var(--accent); }

    /* hamburger */
    .hamburger {
      display: none;
      background: none;
      border: none;
      color: var(--fg);
      cursor: pointer;
      padding: 0.25rem;
    }
    @media (max-width: 767px) {
      .nav-links { display: none; }
      .hamburger { display: block; }
    }
    .mobile-nav {
      display: none;
      flex-direction: column;
      gap: 1.5rem;
      background: var(--card);
      border-top: 1px solid var(--border);
      padding: 2rem 1.5rem;
    }
    .mobile-nav.open { display: flex; }
    .mobile-nav a {
      font-size: 1rem;
      color: var(--muted-fg);
    }
    .mobile-nav .btn-primary { text-align: center; justify-content: center; }

    /* ─── HERO ─── */
    .hero {
      padding: var(--pad-section) 0 var(--pad-section-sm);
    }
    @media (min-width: 1024px) { .hero { padding: var(--pad-section-lg) 0 var(--pad-section); } }

    .hero-grid {
      display: grid;
      gap: 2.5rem;
      align-items: end;
    }
    @media (min-width: 1024px) {
      .hero-grid {
        grid-template-columns: repeat(12, 1fr);
      }
      .hero-headline { grid-column: 1 / 9; }
      .hero-sub { grid-column: 9 / 13; padding-bottom: 0.25rem; }
    }

    .hero h1 {
      font-family: var(--display);
      font-weight: 300;
      font-size: clamp(2.75rem, 7vw, 5.25rem);
      line-height: 1.04;
      letter-spacing: -0.02em;
    }
    .hero h1 em {
      font-style: normal;
      color: var(--accent);
    }
    .hero-sub p {
      color: var(--muted-fg);
      font-size: 1.0625rem;
      line-height: 1.65;
      margin-bottom: 2rem;
    }
    .hero-trust {
      margin-top: 5rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border);
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    @media (min-width: 640px) {
      .hero-trust { flex-direction: row; align-items: center; gap: 2rem; }
    }
    .hero-trust .label-sm {
      font-family: var(--mono);
      font-size: 0.6875rem;
      text-transform: uppercase;
      letter-spacing: 0.16em;
      color: var(--muted-fg);
      white-space: nowrap;
    }
    .hero-trust p {
      font-size: 1rem;
      color: var(--muted-fg);
      line-height: 1.6;
    }

    /* ─── SHARED SECTION TITLE ROW ─── */
    .section-header {
      display: grid;
      gap: 3rem;
      margin-bottom: 4rem;
    }
    @media (min-width: 1024px) {
      .section-header { grid-template-columns: repeat(12, 1fr); }
      .section-header .sh-left { grid-column: 1 / 7; }
      .section-header .sh-right { grid-column: 8 / 13; display: flex; align-items: flex-end; }
    }

    .section-h2 {
      font-family: var(--display);
      font-weight: 300;
      font-size: clamp(1.75rem, 3.5vw, 2.5rem);
      line-height: 1.2;
      margin-top: 1.25rem;
    }

    /* ─── PAIN POINTS ─── */
    .pain-section {
      padding: 6rem 0;
      border-top: 1px solid var(--border);
    }
    @media (min-width: 1024px) { .pain-section { padding: 8rem 0; } }

    .pain-grid {
      display: grid;
      gap: 3rem;
    }
    @media (min-width: 1024px) {
      .pain-grid { grid-template-columns: repeat(12, 1fr); }
      .pain-left { grid-column: 1 / 5; }
      .pain-right { grid-column: 6 / 13; }
    }

    .pain-list { list-style: none; }
    .pain-item {
      display: flex;
      align-items: flex-start;
      gap: 1.5rem;
      padding: 1.25rem 0;
      border-top: 1px solid var(--border);
      cursor: default;
      transition: color 0.2s;
    }
    .pain-item:hover { color: var(--accent); }
    .pain-num {
      font-family: var(--mono);
      font-size: 0.625rem;
      color: var(--muted-fg);
      padding-top: 0.375rem;
      min-width: 1.75rem;
      flex-shrink: 0;
    }
    .pain-text { font-size: 1.0625rem; line-height: 1.4; }

    /* ─── METHODOLOGY ─── */
    .method-section {
      padding: 6rem 0;
      background: var(--card);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
    }
    @media (min-width: 1024px) { .method-section { padding: 8rem 0; } }

    .method-grid {
      display: grid;
      gap: 1px;
      background: var(--border);
    }
    @media (min-width: 1024px) { .method-grid { grid-template-columns: repeat(3, 1fr); } }

    .method-card {
      background: var(--card);
      padding: 2rem 2.5rem;
      cursor: default;
    }
    @media (min-width: 1024px) { .method-card { padding: 3rem; } }
    .method-num {
      font-family: var(--mono);
      font-size: 0.625rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--accent);
      margin-bottom: 2.5rem;
    }
    .method-title {
      font-family: var(--display);
      font-weight: 300;
      font-size: clamp(1.5rem, 2.5vw, 1.875rem);
      margin-bottom: 1rem;
      transition: color 0.3s;
    }
    .method-card:hover .method-title { color: var(--accent); }
    .method-desc { color: var(--muted-fg); font-size: 1rem; line-height: 1.7; }

    /* ─── PHILOSOPHY ─── */
    .philosophy-section {
      padding: 6rem 0;
      border-bottom: 1px solid var(--border);
    }
    @media (min-width: 1024px) { .philosophy-section { padding: 8rem 0; } }

    .phil-grid {
      display: grid;
      gap: 1px;
      background: var(--border);
    }
    @media (min-width: 640px)  { .phil-grid { grid-template-columns: repeat(2, 1fr); } }
    @media (min-width: 1024px) { .phil-grid { grid-template-columns: repeat(4, 1fr); } }

    .phil-card {
      background: var(--bg);
      padding: 2rem 2.5rem;
      cursor: default;
    }
    @media (min-width: 1024px) { .phil-card { padding: 2.5rem; } }
    .phil-title {
      font-family: var(--display);
      font-weight: 300;
      font-size: 1.25rem;
      margin-bottom: 0.75rem;
      transition: color 0.2s;
    }
    .phil-card:hover .phil-title { color: var(--accent); }
    .phil-desc { color: var(--muted-fg); font-size: 0.8125rem; line-height: 1.7; }

    /* ─── TWO WAYS ─── */
    .ways-section {
      padding: 6rem 0;
      background: var(--card);
      border-bottom: 1px solid var(--border);
    }
    @media (min-width: 1024px) { .ways-section { padding: 8rem 0; } }

    .ways-grid {
      display: grid;
      gap: 1px;
      background: var(--border);
    }
    @media (min-width: 1024px) { .ways-grid { grid-template-columns: repeat(2, 1fr); } }

    .way-card {
      display: flex;
      flex-direction: column;
      padding: 2.5rem;
    }
    @media (min-width: 1024px) { .way-card { padding: 3.5rem; } }
    .way-card:first-child { background: var(--card); }
    .way-card:last-child { background: var(--secondary); }

    .way-num {
      font-family: var(--mono);
      font-size: 0.625rem;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--accent);
      margin-bottom: 1.5rem;
    }
    .way-title {
      font-family: var(--display);
      font-weight: 300;
      font-size: clamp(1.5rem, 2.5vw, 1.875rem);
      margin-bottom: 1.25rem;
    }
    .way-desc {
      color: var(--muted-fg);
      font-size: 1rem;
      line-height: 1.7;
      margin-bottom: 2.5rem;
      flex: 1;
    }

    /* ─── CTA ─── */
    .cta-section {
      padding: 8rem 0;
      text-align: center;
    }
    @media (min-width: 1024px) { .cta-section { padding: 11rem 0; } }
    .cta-inner { max-width: 48rem; margin: 0 auto; }
    .cta-inner .label { justify-content: center; }
    .cta-inner .label.center::before { content: ''; display: inline-block; width: 1.5rem; height: 1px; background: var(--accent); }

    .cta-h2 {
      font-family: var(--display);
      font-weight: 300;
      font-size: clamp(2.25rem, 5vw, 3.75rem);
      line-height: 1.08;
      letter-spacing: -0.02em;
      margin: 2rem 0 1.5rem;
    }
    .cta-sub {
      color: var(--muted-fg);
      font-size: 1.0625rem;
      line-height: 1.65;
      margin-bottom: 3rem;
    }

    /* ─── FOOTER ─── */
    footer {
      border-top: 1px solid var(--border);
      padding: 2rem 0;
    }
    .footer-inner {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    @media (min-width: 640px) {
      .footer-inner { flex-direction: row; align-items: center; justify-content: space-between; }
    }
    .footer-copy {
      font-family: var(--mono);
      font-size: 0.6875rem;
      color: var(--muted-fg);
      letter-spacing: 0.06em;
    }

    /* ─── SVG arrow ─── */
    .arrow { display: inline-block; transition: transform 0.2s; }
    a:hover .arrow { transform: translateX(3px); }

    /* ─── Utility ─── */
    .mb-label { margin-bottom: 1.25rem; }
    .mb-4 { margin-bottom: 1rem; }
    .mb-8 { margin-bottom: 2rem; }
  </style>

  <!-- ── HERO ── -->
  <section class="hero">
    <div class="container">
      <div class="hero-grid">
        <div class="hero-headline">
          <div class="label mb-label">CRO &amp; UX Research Agency</div>
          <h1>Turn user insights<br><em>into revenue</em></h1>
        </div>
        <div class="hero-sub">
          <p>Convert more customers with expert research, strategy, design and implementation — all under one roof.</p>
          <a href="#contact" class="btn-primary">
            Let's Chat
            <svg class="arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>
      </div>

      <div class="hero-trust">
        <span class="label-sm">Trusted by</span>
        <p>SaaS and ecommerce companies rely on us to help solve their digital challenges.</p>
      </div>
    </div>
  </section>

  <!-- ── PAIN POINTS ── -->
  <section class="pain-section" id="services">
    <div class="container">
      <div class="pain-grid">
        <div class="pain-left">
          <div class="label mb-label">What We Solve</div>
          <h2 class="section-h2">Find where visitors are getting stuck</h2>
        </div>
        <div class="pain-right">
          <ul class="pain-list">
            <li class="pain-item">
              <span class="pain-num">01</span>
              <span class="pain-text">Improve product pages, landing pages, forms, and checkout flows</span>
            </li>
            <li class="pain-item">
              <span class="pain-num">02</span>
              <span class="pain-text">Clarify messaging and calls to action</span>
            </li>
            <li class="pain-item">
              <span class="pain-num">03</span>
              <span class="pain-text">Strengthen trust signals and decision support</span>
            </li>
            <li class="pain-item">
              <span class="pain-num">04</span>
              <span class="pain-text">Prioritize the changes most likely to improve conversion</span>
            </li>
            <li class="pain-item">
              <span class="pain-num">05</span>
              <span class="pain-text">Turn insights into practical website improvements</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- ── METHODOLOGY ── -->
  <section class="method-section" id="process">
    <div class="container">
      <div class="section-header" style="margin-bottom:5rem;">
        <div class="sh-left">
          <div class="label mb-label">Our Approach</div>
          <h2 class="section-h2">A research-driven methodology that systematically improves digital experiences</h2>
        </div>
        <div class="sh-right">
          <p style="color:var(--muted-fg);font-size:0.9375rem;line-height:1.7;">
            With 20+ years of experience, we help you move from guesswork to evidence — turning observations into measurable action.
          </p>
        </div>
      </div>

      <div class="method-grid">
        <div class="method-card">
          <div class="method-num">01</div>
          <div class="method-title">Evaluate</div>
          <p class="method-desc">Start with a focused CRO Conversion Audit to identify where your site is losing sales or leads and what to fix first.</p>
        </div>
        <div class="method-card">
          <div class="method-num">02</div>
          <div class="method-title">Uncover</div>
          <p class="method-desc">Go deeper with collaborative UX workshops that reveal customer needs, buying barriers, stakeholder assumptions, and opportunities across the full customer journey.</p>
        </div>
        <div class="method-card">
          <div class="method-num">03</div>
          <div class="method-title">Implement</div>
          <p class="method-desc">Move from recommendations to action with hands-on support for page improvements, content direction, UX refinements, testing plans, and coordination with your team.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ── PHILOSOPHY ── -->
  <section class="philosophy-section">
    <div class="container">
      <div style="margin-bottom:4rem;">
        <div class="label mb-label">Why It Matters</div>
        <h2 class="section-h2" style="max-width:36rem;">A healthy optimization program is more than just increasing a conversion rate</h2>
      </div>

      <div class="phil-grid">
        <div class="phil-card">
          <div class="phil-title">Scale sustainably</div>
          <p class="phil-desc">Iterative improvements that compound over time without burning your team.</p>
        </div>
        <div class="phil-card">
          <div class="phil-title">Data-backed decisions</div>
          <p class="phil-desc">Fast, evidence-informed changes that reduce guesswork and stakeholder friction.</p>
        </div>
        <div class="phil-card">
          <div class="phil-title">Stay user-centered</div>
          <p class="phil-desc">Solutions grounded in how real customers think, feel, and decide.</p>
        </div>
        <div class="phil-card">
          <div class="phil-title">Communicate your Why</div>
          <p class="phil-desc">Sharpen your story and brand messaging so it resonates and converts.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ── TWO WAYS TO START ── -->
  <section class="ways-section">
    <div class="container">
      <div style="margin-bottom:4rem;">
        <div class="label mb-label">Two Ways to Start</div>
        <h2 class="section-h2">Low risk. High reward.</h2>
      </div>

      <div class="ways-grid">
        <div class="way-card">
          <div class="way-num">Option 01</div>
          <div class="way-title">Conversion Audit</div>
          <p class="way-desc">A quick audit will uncover targeted quick-win projects. Get clear priorities, practical fixes, and momentum without a large upfront commitment.</p>
          <a href="#contact" class="btn-outline" style="align-self:flex-start;">
            Start with an audit
            <svg class="arrow" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>
        <div class="way-card">
          <div class="way-num">Option 02</div>
          <div class="way-title">Deeper Discovery</div>
          <p class="way-desc">If your customer journey is complex, your team needs alignment, or you want deeper insight before making bigger changes — we offer a comprehensive evaluation and collaborative workshop process.</p>
          <a href="#contact" class="btn-outline" style="align-self:flex-start;">
            Explore the full process
            <svg class="arrow" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ── CTA ── -->
  <section class="cta-section" id="contact">
    <div class="container">
      <div class="cta-inner">
        <div class="label center" style="justify-content:center;">Get Started</div>
        <h2 class="cta-h2">Ready to find out where your website is losing customers?</h2>
        <p class="cta-sub">Let's talk through your options and find a way forward that fits your goals.</p>
        <a href="mailto:hello@clearfield.studio" class="btn-primary" style="font-size:0.9375rem;padding:1rem 2rem;">
          Contact Us
          <svg class="arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ── FOOTER ── -->
  <footer>
    <div class="container">
      <div class="footer-inner">
        <span class="wordmark">Atl House Interactive</span>
        <span class="footer-copy">© <span id="year"></span> · CRO &amp; UX Research</span>
      </div>
    </div>
  </footer>

  <script>
    // Year
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>